// The Settings page is now a sectioned layout with nested routes — see
// `frontend/src/pages/settings/`. This file re-exports for backwards-compat
// with any stale imports.
export { SettingsLayout as Settings } from './settings/SettingsLayout'
