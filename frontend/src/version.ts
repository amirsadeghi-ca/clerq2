/**
 * App version — the single source of truth.
 *
 * Bump this on EVERY meaningful change before committing:
 *   MAJOR (x.0.0) — breaking changes or full redesigns
 *   MINOR (0.x.0) — new user-facing features
 *   PATCH (0.0.x) — bug fixes, small tweaks, copy/style changes
 *
 * Displayed in the left sidebar. Referenced in CLAUDE.md.
 */
export const APP_VERSION = '1.4.0'
